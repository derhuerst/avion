{
	"name": "avion",
	"description": "Fast and easy file transfer without the NSA.",
	"version": "0.2.0",
	"keywords": [
		"avion",
		"transfer",
		"files",
		"webrtc",
		"p2p"
	],
	"author": "Jannis R <mail@jannisr.de>",
	"homepage": "https://github.com/derhuerst/avion",
	"repository": "derhuerst/avion",
	"issues": "https://github.com/derhuerst/avion/issues",
	"license": "ISC",
	"dependencies": {
		"component-emitter": "^1.2.1",
		"file-saver": "^1.3.3",
		"lodash.every": "^4.6.0",
		"lodash.find": "^4.6.0",
		"shorthash": "^0.0.2",
		"signalhub": "^4.8.0",
		"simple-peer": "^8.1.0"
	},
	"devDependencies": {
		"browserify": "^14.1.0",
		"uglify-es": "^3.0.12",
		"wzrd": "^1.5.0"
	},
	"scripts": {
		"bundle": "browserify index.js > bundle.js",
		"uglify": "uglifyjs -mc --toplevel -- bundle.js > bundle.min.js",
		"build": "npm run bundle && npm run uglify",
		"prepublishOnly": "npm run build",
		"dev": "wzrd index.js:bundle.min.js -- --debug"
	}
}
